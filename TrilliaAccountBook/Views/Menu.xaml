<UserControl x:Class="TrilliaAccountBook.Views.Menu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"             
             xmlns:mah="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid>
        <StackPanel Grid.Column="2" 
                    Grid.Row="1"
                    Width="200">
            <Button Content="Journal Register" Height="50" FontSize="16" Margin="4" Command="{Binding JournalEntryCommand}" />
            <Button Content="" Height="50" FontSize="16" Margin="4" Command="{Binding SalesEntryCommand}" />
            <Button Content="" Height="50" FontSize="16" Margin="4" Command="{Binding PaymentEntryCommand}"/>
            <Button Content="" Height="50" FontSize="16" Margin="4" Command="{Binding InvoiceCommand}"/>

        </StackPanel>

    </Grid>
</UserControl>
